# MongoDB connection settings for signup_database container
# These variables are consumed by init_db.js and can be sourced into the environment.
# Do NOT commit real credentials; this is an example.

MONGODB_URL="mongodb://appuser:dbuser123@localhost:5000/?authSource=admin"
MONGODB_DB="myapp"
